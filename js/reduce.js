const numbers = [4, 5, 6, 7 , 8, 10 ,66];
const total = numbers.reduce((prevous, current) => prevous + current ,0)
console.log(total);












const totalPrice = numbers.reduce((p, c) => p + c, 0);
console.log(totalPrice);